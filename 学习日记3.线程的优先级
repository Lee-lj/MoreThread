Java提供线程调度器来监控程序中启动后进入就绪状态的所有线程，线程调度器按照优先级决定应该调度哪个线程来执行

线程优先级用数字表示，范围0~10
   Thread.MIN_PRIORITY=1,
   Thread.MAX_PRIORITY=10;
   Thread.NORM_PRIORITY=5;默认线程优先级为5
   使用以下方式改变或获取优先级
   getPriority().setPriority(int x)
   
   设置优先级，仅能作为参考，无法决定线程的最终执行顺序，设置的值越高，优先级越大，可能会提前执行
   
   优先级低只是意味着获得调度的概率低，并不是优先级低就不会先调用，都得看CPU的调度顺序
   
   性能倒置:设置了低优先级的被先执行，或设置了高优先级的被后执行